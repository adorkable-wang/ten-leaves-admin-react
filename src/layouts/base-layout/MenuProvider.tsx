import type { FC, ReactNode } from "react";

interface Props {
  children: ReactNode;
}

const MenuProvider: FC<Props> = ({ children }) => {
  return <>hh</>;
};

export default MenuProvider;
